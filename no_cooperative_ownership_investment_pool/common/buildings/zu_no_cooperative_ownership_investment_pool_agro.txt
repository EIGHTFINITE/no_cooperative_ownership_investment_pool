############# FARMING & RANCHING

INJECT:building_rye_farm = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}

INJECT:building_wheat_farm = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}

INJECT:building_rice_farm = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}

INJECT:building_maize_farm = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}

INJECT:building_millet_farm = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}

INJECT:building_livestock_ranch = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}


INJECT:building_vineyard = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}
