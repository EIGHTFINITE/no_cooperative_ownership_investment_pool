INJECT:building_logging_camp = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}

INJECT:building_rubber_plantation = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}

INJECT:building_fishing_wharf = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}

INJECT:building_whaling_station = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}


INJECT:building_oil_rig = {
	can_build_private = {
		exists = owner
		exists = scope:investor_country
		NAND = {
			owner = scope:investor_country
			OR = {
				bring_back_directly_controlled_investment_pool = yes
				AND = {
					no_cooperative_ownership_investment_pool = yes
					owner = {
						OR = {
							has_law = law_type:law_cooperative_ownership
							has_law = law_type:law_command_economy
						}
					}
				}
			}
		}
	}
}
